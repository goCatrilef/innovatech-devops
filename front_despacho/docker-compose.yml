services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: innovatech-frontend:latest
    container_name: innovatech-frontend
    ports:
      - "8080:80"
    restart: always